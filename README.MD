🤖 Telegram Bot с локальной ИИ-моделью + фоллбек на OpenRouter
Этот Telegram-бот использует локальную нейросеть через Ollama для ответов на сообщения, а в случае её недоступности — автоматически переключается на OpenRouter (модель z-ai/glm-4.5-air:free).

💡 Особенности
✅ Автоматический фоллбек: если Ollama не запущена или ноутбук выключен — бот отвечает через облачную модель.
✅ Умный UX: показывает статус "ща отвечу 👌" во время обработки и удаляет его при получении ответа.
✅ Поддержка только текста: игнорирует стикеры, фото и другие медиа с вежливым сообщением.
✅ Безопасная работа: все ключи хранятся в .env, ошибки обрабатываются без падений.
✅ Лёгкий и быстрый: написан на Node.js с использованием ES-модулей, Telegraf и Axios.
🚀 Как запустить
Установи Ollama и скачай модель:

ollama pull qwen3:4b
Получи API-ключ от OpenRouter и сохрани его в .env:

BOT_TOKEN=your_telegram_bot_token
OPENROUTER_API_KEY=your_openrouter_api_key
Установи зависимости:

npm install telegraf axios dotenv ollama
Запусти бота:

npm start
💡 Важно: Ollama должен быть запущен (ollama serve) — иначе бот автоматически использует OpenRouter. 

🛠 Технологии
Node.js (ES-модули)
Telegraf.js — фреймворк для Telegram-ботов
Ollama — локальная LLM-платформа
OpenRouter — единый API для десятков моделей
Axios & Fetch — HTTP-запросы
dotenv — управление переменными окружения
